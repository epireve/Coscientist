# Coscientist secrets — copy to .env and fill in.
# .env is gitignored. NEVER commit real values.

# University Malaya (UM) institutional access via OpenAthens
# Used by .claude/skills/institutional-access/scripts/idp_um.py
UM_USERNAME=
UM_PASSWORD=

# Zenodo dataset deposit (real or sandbox)
# ZENODO_TOKEN=
# ZENODO_SANDBOX_TOKEN=
